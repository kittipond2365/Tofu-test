import type { PlayerStatsResponse } from '@/lib/types';
export function LeaderboardTable({ data }: { data: PlayerStatsResponse[] }) { const sorted=[...data].sort((a,b)=>b.rating-a.rating); return <div className="overflow-auto"><table className="min-w-full text-sm"><thead><tr className="text-left"><th>#</th><th>Player</th><th>Rating</th><th>W/L</th><th>Win%</th></tr></thead><tbody>{sorted.map((p,i)=><tr key={p.user_id} className="border-t"><td>{i+1}</td><td>{p.display_name||p.full_name}</td><td>{p.rating}</td><td>{p.wins}/{p.losses}</td><td>{p.win_rate}</td></tr>)}</tbody></table></div>; }

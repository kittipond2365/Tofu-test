# Badminton Club App - Environment Variables

# ============================================
# Required: LINE Login Configuration
# Get from: https://developers.line.biz/
# ============================================
LINE_CHANNEL_ID=your-line-channel-id
LINE_CHANNEL_SECRET=your-line-channel-secret
LINE_REDIRECT_URI=http://localhost:3000/auth/line/callback

# ============================================
# Optional: Database (auto-configured in Docker)
# ============================================
# DATABASE_URL=postgresql+asyncpg://badminton:badminton123@postgres:5432/badminton_db
# DATABASE_URL_SYNC=postgresql://badminton:badminton123@postgres:5432/badminton_db

# ============================================
# Optional: Redis (auto-configured in Docker)
# ============================================
# REDIS_URL=redis://redis:6379/0

# ============================================
# Security (change in production!)
# ============================================
SECRET_KEY=your-secret-key-here-change-in-production

# ============================================
# Web Push Notifications (VAPID - NOT Firebase!)
# ============================================
# Note: We use Web Push VAPID standard, NOT Firebase Cloud Messaging
# Generate VAPID keys: https://vapidkeys.com/ or use web-push library
WEB_PUSH_ENABLED=false
WEB_PUSH_VAPID_PUBLIC_KEY=your-vapid-public-key
WEB_PUSH_VAPID_PRIVATE_KEY=your-vapid-private-key
WEB_PUSH_VAPID_CLAIMS_EMAIL=your-email@example.com

# Frontend VAPID Public Key (must match backend)
# NEXT_PUBLIC_WEB_PUSH_VAPID_PUBLIC_KEY=your-vapid-public-key

# ============================================
# Email SMTP (for notifications)
# ============================================
EMAIL_ENABLED=false
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASSWORD=your-app-password
SMTP_FROM_EMAIL=your-email@gmail.com
